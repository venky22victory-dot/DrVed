# Frontend (React) - DrVed

This is a small React app with product listing, cart, and checkout integration with Razorpay.

- Set `REACT_APP_API_BASE_URL` to your backend URL.
- Set `REACT_APP_RAZORPAY_KEY_ID` to your Razorpay public key id.
