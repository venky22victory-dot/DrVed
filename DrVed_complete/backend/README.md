# Backend (Express) - DrVed

## Features
- Products endpoints (list, get)
- Razorpay order creation and signature verification endpoints
- LowDB JSON used for demo product storage

## Setup
1. Copy `.env.example` to `.env` and fill in keys.
2. `npm install`
3. `npm run dev`
