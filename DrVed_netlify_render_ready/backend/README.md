# Backend (Express) - DrVed (Render-ready)

- Set environment variables: RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET, PUBLIC_URL (frontend URL)
- Start command: `npm start`
- For Render, use `Node` environment and point root to this `backend` folder.
